<%= form_for(@funcao, html: { class: 'form-horizontal' }) do |f| %>
  <%#= render 'layouts/error_messages', model_name: @funcao %>
  <%= render 'layouts/message_required' %>
  <div class="form-group">
    <div class="col-sm-10 col-offset-1">
      <%= f.label :nome %>
      <%= f.text_field :nome, class: 'form-control' %>
    </div>
  </div>
  <!--<div class="form-group">
    <div class="col-sm-10 col-offset-1">
      <%#= f.label :Area %>
      <%#= f.select :area_id, options_for_select(Area.joins(:unidade_socioeducativa).all.collect {|e| [(e.unidade_socioeducativa.sigla + '-' + e.nome), e.id]}, {prompt: "selecione"}), {class: "form-control"} %>
    </div>
  </div>-->
  <div class="form-group">
    <div class="col-sm-10">
      <%= f.submit nil, class: 'btn btn-primary', data: { disable_with: disable_with } %>
      <%= link_to I18n.t('helpers.links.cancel'), funcoes_path, class: 'btn btn-warning' %>
    </div>
  </div>
<% end %>
