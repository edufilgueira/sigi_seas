$('#modalAuxiliarTitle').html('Novo contato')
$('#modalAuxiliar').find('.modal-body').html("<%= escape_javascript( render partial: 'form', locals: { disable_with: 'Salvando...' } ) %>");
$('#modalAuxiliar').modal()
