<% content_for :title do %>
  <%= t('.title', default: @status_patrimonio.class.model_name.human ) %>
<% end -%>
<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5><%= t('.title', default: @status_patrimonio.class.model_name.human ) %></h5>
        <div class="ibox-tools">
          <a class="collapse-link">
            <i class="fa fa-chevron-up"></i>
          </a>
          <a class="close-link">
            <i class="fa fa-times"></i>
          </a>
        </div>
      </div>
      <div class="ibox-content">
        <div class="row">
          <div class="col-sm-12 b-r">
            <table class="table table-condensed">
              <thead>
                <tr>
                                    <th><%= StatusPatrimonio.human_attribute_name :nome %></th>
                                    <th><%= StatusPatrimonio.human_attribute_name :descricao %></th>
                                  </tr>
              </thead>
              <tbody>
                <tr>
                                    <td style="min-width: 90px; word-break: break-all;"><%= @status_patrimonio.nome %></td>
                                    <td style="min-width: 90px; word-break: break-all;"><%= @status_patrimonio.descricao %></td>
                                  </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to('Listar', status_patrimonios_path, class: 'btn btn-default') %>
<%= link_to 'Editar', edit_status_patrimonio_path(@status_patrimonio), class: 'btn btn-detran-primary' %>
