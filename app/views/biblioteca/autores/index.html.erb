<%- model_class = Biblioteca::Autor -%>
<% content_for :title do %>
<%=  I18n.t('activerecord.models.biblioteca_autor', default: Biblioteca::Autor.model_name.human.pluralize.titleize) %>
<% end -%>

<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5><%=  I18n.t('activerecord.models.biblioteca_autor', default: Biblioteca::Autor.model_name.human.pluralize.titleize) %></h5>
        <div class="ibox-tools">
          <a class="collapse-link">
            <i class="fa fa-chevron-up"></i>
          </a>
          <a class="close-link">
            <i class="fa fa-times"></i>
          </a>
        </div>
      </div>
      <div class="ibox-content">
        <div class="row">
          <div class="col-sm-12 b-r">
            <%= link_to_new new_biblioteca_autor_path %>
            <table class="table table-hover table-sm">
              <thead>
                <tr>
                                    <th><%= model_class.human_attribute_name(:nome) %></th>
                                    <th width="18%"><%= I18n.t('helpers.actions') %></th>
                </tr>
              </thead>
              <tbody>
                <% @biblioteca_autores.each do |biblioteca_autor| %>
                <tr>
                                    <td><%= biblioteca_autor.nome %></td>
                                    <td>
                    <%= link_to_show(biblioteca_autor) %>
                    <%= link_to_edit(edit_biblioteca_autor_path(biblioteca_autor)) %>
                    <%= link_to_delete(biblioteca_autor) %>
                  </td>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
