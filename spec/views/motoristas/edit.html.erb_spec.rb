require 'rails_helper'

RSpec.describe "motoristas/edit", type: :view do
  before(:each) do
    #@motoristas = assign(:motorista, FactoryBot.create(:motorista))
  end

  #it "renders the edit motorista form" do
    #render

    #assert_select "form[action=?][method=?]", motorista_path(@motorista), "post" do

     #assert_select "selecet[name=?]", "motorista[motorista]"

     #assert_select "input[name=?]", "motorista[numero]"

     #assert_select "input[name=?]", "motorista[registro]"

      #assert_select "input[name=?]", "motorista[categoria]"

      #assert_select "textarea[name=?]", "motorista[obs]"
    #end
  #end
end
