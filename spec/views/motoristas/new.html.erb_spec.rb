require 'rails_helper'

RSpec.describe "motoristas/new", type: :view do
  before(:each) do
    #@motoristas = assign(:motorista, FactoryBot.build(:motorista))
    
  end

  #it "renders new motorista form" do
    #render

    #assert_select "form[action=?][method=?]", motoristas_path, "post" do

      #assert_select "select[name=?]", "motorista[pessoa_id]"

      #assert_select "input[name=?]", "motorista[numero]"

      #assert_select "input[name=?]", "motorista[registro]"

      #assert_select "input[name=?]", "motorista[categoria]"

      #assert_select "textarea[name=?]", "motorista[obs]"
    #end
  #end
end
