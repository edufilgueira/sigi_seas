require 'rails_helper'

RSpec.describe "motoristas/show", type: :view do
  before(:each) do
      #@motoristas = assign(:motorista, FactoryBot.create(:motorista))
  end

  #it "renders attributes in <p>" do
    #render
    #expect(rendered).to match(/MyString/)
    #expect(rendered).to match(/MyString/)
    #expect(rendered).to match(/MyString/)
    #expect(rendered).to match(/MyString/)
    #expect(rendered).to match(/MyText/)
  #end
end
